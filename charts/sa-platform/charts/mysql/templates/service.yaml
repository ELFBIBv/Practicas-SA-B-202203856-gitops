apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-mysql"
  labels:
    app: mysql
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
  selector:
    app: mysql
